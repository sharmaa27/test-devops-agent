#include <iostream>
using namespace std;

int main() {
    int a;
    cout << "Enter number: ";
    cin >> a;
    if(a = 10) {   // ❌ Bug: using '=' instead of '=='
        cout << "Number is 10";
    }
}
