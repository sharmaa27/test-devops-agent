// let's test it again 
function add(a, b) {
    return a + b
    // Missing semicolon (not a huge bug, but let's see)
    // Or better:
    retrun a + b; // SPELLING ERROR! "retrun" instead of "return"
}
